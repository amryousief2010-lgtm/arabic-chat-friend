## التغييرات المطلوبة

### 1. تحويل الصفحة الافتراضية للمسوقات إلى صفحة الطلبات
- في `src/constants/roleLandings.ts`: تغيير `sales_moderator` من `/org-chart` إلى `/orders`.
- في `src/components/ProtectedRoute.tsx`: تحديث `moderatorTarget` للمسوقة ليصبح `/orders` بدلاً من `/org-chart`، مع الإبقاء على `/org-chart` ضمن الصفحات المسموح بزيارتها.

### 2. تغيير تسمية فلتر "قيد التوصيل" إلى "قيد الانتظار"
- في `src/pages/Orders.tsx` (السطر 643): تغيير نص الزر من `قيد التوصيل` إلى `قيد الانتظار` (الفلتر يبقى مرتبطاً بحالة `shipped` بدون تغيير في المنطق).

### ملاحظات
- الهيكل التنظيمي `/org-chart` يبقى متاحاً للمسوقات عبر القائمة، لكن لن يكون هو الصفحة الأولى بعد تسجيل الدخول.
- لا تغييرات على قاعدة البيانات أو منطق الحالات.